When the user clicks the Google Reviews link:

If the current page contains a reviews section, scroll or navigate to the reviews section on that same page.

If the current page does NOT contain any reviews, redirect the user to the home page reviews section.

The solution should work consistently across all site pages.

Avoid hard-coding page URLs where possible; detect the presence of the reviews section dynamically.

Ensure smooth scrolling and no broken links or console errors.

make sure we might need to revert if not properly done

then execute